{% extends 'front/back.html.twig' %}



{% block body %}
    <div class="boxMD row">
    <h1>Modèle</h1>

    <table class="table table table-success table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ model.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ model.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ model.description }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('app_back_model_index') }}">Retour à la liste</a>
    </div>

    

    {{ include('back_model/_delete_form.html.twig') }}
{% endblock %}
