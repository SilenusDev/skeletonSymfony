<!DOCTYPE html>

<title>New Brand</title>

{% block body %}
    <h1>Create new Brand</h1>

    {{ include('back_brand/_form.html.twig') }}

    <a href="{{ path('app_back_brand_index') }}">back to list</a>
{% endblock %}
