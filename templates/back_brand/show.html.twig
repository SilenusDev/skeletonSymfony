{% extends 'front/back.html.twig' %}

{% block body %}

{% block body %}
<div class="boxSM row">
    <h1>Marque</h1>

    <table class="table table-success table-striped table-hover">
        <tbody>
            
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
        <a href="{{ path('app_back_brand_index') }}">back to list</a>
    </table>
</div>    

    

    

    {{ include('back_brand/_delete_form.html.twig') }}
{% endblock %}
