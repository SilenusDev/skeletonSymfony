<!DOCTYPE html>

<title>Edit Listing</title>

{% block body %}
    <h1>Edit Listing</h1>

    {{ include('front_listing/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_front_listing_index') }}">back to list</a>

    {{ include('front_listing/_delete_form.html.twig') }}
{% endblock %}
