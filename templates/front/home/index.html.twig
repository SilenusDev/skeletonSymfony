{% extends 'front/base.html.twig' %}

{% block body %}


<div class="boxXL row">
<h1> Toutes les annonces</h1>
        <div class="navigation d-flex justify-content-center mt-2">
            {{ knp_pagination_render(listings) }}
        </div>
          {% for listing in listings %}
            <div class="card col-12 col-md-3 m-5">
            <img src="https://fr.muzeo.com/sites/default/files/styles/image_moyenne_def/public/oeuvres/photo/les_deebuts/la_charette_ea_eau139458.jpg?itok=BRDJPfIF" class="card-img-top" alt="...">
        
        <div class="card-body">
          <h5 class="card-title">{{ listing.title }}</h5>
          <p class="card-text">{{ listing.description }}</p>
          <a href="{{ path('app_show', {'id': listing.id}) }}" class="btn btn-success">Voir la charette</a>
        </div>
      </div>
                {# <div class="col-12 col-md-3">
                <h5 src="" class="card-title">{{ listing.title }}</h5>
                
                </div> #}
            {% endfor %}
            <div class="navigation d-flex justify-content-center mt-2">
            {{ knp_pagination_render(listings) }}
        </div>
        </div>
   </div>     
{% endblock %}
